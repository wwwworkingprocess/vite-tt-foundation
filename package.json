{
  "name": "torrevieja-tycoon",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "yarn@4.17.1",
  "engines": {
    "node": "24.18.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --all --topological run build",
    "build:libraries": "yarn workspace @torrevieja-tycoon/protocol build && yarn workspace @torrevieja-tycoon/simulation build",
    "dev": "yarn build:libraries && yarn workspace @torrevieja-tycoon/web dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "yarn build:libraries && vitest run",
    "test:coverage": "yarn build:libraries && vitest run --project simulation --coverage && vitest run --project protocol --coverage && vitest run --project web --coverage",
    "typecheck": "tsc --build",
    "test:e2e": "yarn build:libraries && start-server-and-test 'yarn workspace @torrevieja-tycoon/web dev --host 127.0.0.1' http://127.0.0.1:4173 'cypress run'",
    "validate": "yarn format:check && yarn lint && yarn typecheck && yarn test && yarn test:coverage && yarn build && yarn test:e2e"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.10.13",
    "@vitest/coverage-v8": "4.1.10",
    "cypress": "15.18.1",
    "eslint": "10.7.0",
    "eslint-plugin-import-x": "4.17.1",
    "globals": "17.4.0",
    "prettier": "3.9.5",
    "start-server-and-test": "2.1.3",
    "typescript": "6.0.3",
    "typescript-eslint": "8.64.0",
    "vitest": "4.1.10"
  }
}

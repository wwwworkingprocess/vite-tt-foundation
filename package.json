{
  "name": "torrevieja-tycoon",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "yarn@4.17.1",
  "engines": {
    "node": "24.18.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --all --topological run build",
    "build:libraries": "yarn workspace @torrevieja-tycoon/protocol build && yarn workspace @torrevieja-tycoon/transport-domain build && yarn workspace @torrevieja-tycoon/simulation build",
    "dev": "yarn build:libraries && yarn workspace @torrevieja-tycoon/web dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "yarn build:libraries && vitest run",
    "test:coverage": "yarn build:libraries && yarn workspace @torrevieja-tycoon/simulation test:coverage && yarn workspace @torrevieja-tycoon/protocol test:coverage && yarn workspace @torrevieja-tycoon/transport-domain test:coverage && yarn workspace @torrevieja-tycoon/web test:coverage",
    "audit:critical-coverage": "node scripts/critical-coverage-audit.mjs",
    "benchmark:population": "yarn build:libraries && node scripts/population-performance-benchmark.mjs 300 --post-only",
    "benchmark:scenario-startup": "yarn build:libraries && node scripts/scenario-startup-benchmark.mjs",
    "benchmark:simulation-runtime": "yarn build:libraries && node scripts/simulation-runtime-benchmark.mjs",
    "benchmark:representation-runtime": "yarn build && start-server-and-test 'yarn workspace @torrevieja-tycoon/web preview --host 127.0.0.1' http://127.0.0.1:4174 'cypress run --config-file cypress.representation.config.ts'",
    "audit:population-fields": "yarn workspace @torrevieja-tycoon/transport-domain build && node scripts/population-crop-audit.mjs",
    "typecheck": "tsc --build",
    "test:e2e": "yarn build:libraries && start-server-and-test 'yarn workspace @torrevieja-tycoon/web dev --host 127.0.0.1' http://127.0.0.1:4173 'cypress run'",
    "test:pwa": "yarn build && start-server-and-test 'yarn workspace @torrevieja-tycoon/web preview --host 127.0.0.1' http://127.0.0.1:4174 'cypress run --config-file cypress.pwa.config.ts'",
    "test:pwa:run": "start-server-and-test 'yarn workspace @torrevieja-tycoon/web preview --host 127.0.0.1' http://127.0.0.1:4174 'cypress run --config-file cypress.pwa.config.ts'",
    "audit:architecture": "node scripts/foundation-audit.mjs",
    "audit:line-endings": "node scripts/line-ending-audit.mjs",
    "audit:tracked": "node scripts/tracked-output-audit.mjs",
    "audit:runtime": "node scripts/runtime-audit.mjs",
    "audit:manifest": "node scripts/template-manifest-audit.mjs",
    "audit:project": "node scripts/project-manifest-audit.mjs",
    "audit:build": "node scripts/build-budget-audit.mjs",
    "audit:subpath": "node scripts/subpath-build-audit.mjs",
    "audit:repository": "node scripts/repository-clean-audit.mjs",
    "test:pwa:subpath": "node scripts/pwa-test-runner.mjs /torrevieja-tycoon/",
    "validate:portable": "yarn audit:runtime && yarn audit:line-endings && yarn audit:architecture && yarn audit:manifest && yarn audit:project && yarn audit:population-fields && yarn format:check && yarn lint && yarn typecheck && yarn test && yarn test:coverage && yarn audit:critical-coverage && yarn build && yarn audit:build && yarn test:e2e && yarn test:pwa && yarn test:pwa:subpath",
    "validate:repository": "yarn audit:tracked && yarn audit:repository",
    "audit": "yarn audit:runtime && yarn audit:architecture && yarn audit:manifest && yarn audit:project && yarn audit:population-fields && yarn audit:build",
    "validate": "yarn audit:population-fields && yarn format:check && yarn lint && yarn typecheck && yarn test && yarn test:coverage && yarn build && yarn test:e2e"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.10.13",
    "@vitest/coverage-v8": "4.1.10",
    "cypress": "15.18.1",
    "eslint": "10.7.0",
    "eslint-plugin-import-x": "4.17.1",
    "globals": "17.4.0",
    "prettier": "3.9.5",
    "start-server-and-test": "2.1.3",
    "typescript": "6.0.3",
    "typescript-eslint": "8.64.0",
    "vitest": "4.1.10"
  }
}
